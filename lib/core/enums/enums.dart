enum PlaybackMode { normal, repeatOne, repeatAll, shuffle }

enum DownloadStatus { pending, downloading, completed, failed, paused }

enum SyncStatus { idle, scanning, syncing, completed, error }

enum StreamingQuality { low, medium, high, lossless }

enum ThemeModePreference { light, dark, system }

enum AudioPlayState { idle, loading, playing, paused, stopped, completed }

enum RepeatMode { off, one, all }
